fragment TeamNode on Team {
  id
  name
  members {
    id
  }
}

fragment GameActionNode on GameScoreAction {
  id
  occurredAt
  occurredBy {
    id
  }
  team {
    ...TeamNode
  }
  value
  key
}

fragment GameNode on Game {
  id
  sport
  status
  scheduledAt
  startedAt
  endedAt
  teams {
    ...TeamNode
  }
  location {
    id
    name
    address {
      id
      street
      city
      state
      stateCode
    }
    geo {
      latitude
      longitude
    }
  }
  actions(first: 20) {
    edges {
      node {
        ...GameActionNode
      }
      cursor
    }

    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}

query GetGame($id: ID!) {
  game(id: $id) {
    ...GameNode
  }
}
