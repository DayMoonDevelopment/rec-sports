mutation SignInWithApple($input: SignInAppleInput!) {
  signInWithApple(input: $input) {
    user {
      id
    }
    session {
      accessToken
      refreshToken
      expiresIn
      expiresAt
      tokenType
    }
  }
}